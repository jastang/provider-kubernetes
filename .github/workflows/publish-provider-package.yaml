name: Publish Provider Package

on:
  workflow_dispatch:
    inputs:
      version:
        description: "Version string to use while publishing the package (e.g. v1.0.0-alpha.1)"
        default: ''
        required: false
      go-version:
        description: 'Go version to use if building needs to be done'
        default: '1.22'
        required: false
      registry_org:
        description: 'Organization name for registry URLs.'
        default: 'crossplane-contrib'
        required: false

jobs:
  publish-provider-package:
    uses: jastang/provider-workflows/.github/workflows/publish-provider-non-family.yml@use-docker-login
    with:
      repository: provider-kubernetes
      version: ${{ github.event.inputs.version }}
      go-version: ${{ github.event.inputs.go-version }}
      cleanup-disk: true
      registry_org: ${{ inputs.registry_org }}
    secrets:
      GHCR_PAT: ${{ secrets.GITHUB_TOKEN }}
      XPKG_UPBOUND_TOKEN: ${{ secrets.XPKG_UPBOUND_TOKEN }}
      XPKG_UPBOUND_ACCESS_ID: ${{ secrets.XPKG_UPBOUND_ACCESS_ID }}
